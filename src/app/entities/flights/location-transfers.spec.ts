import { FlightCompany } from './flight-company';
import { LocationTransfers } from './location-transfers';

describe('LocationTransfers', () => {
  it('should create an instance', () => {
    expect(new LocationTransfers()).toBeTruthy();
  });
});
