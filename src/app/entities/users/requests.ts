import { User } from './user';

export class Requests {
    id: string;
    userId: string;
    user: User;
    friendId: string;
    friend: User;
    status: number;

    constructor()
    {
    }
}